import { View } from "react-native";
import GoogleAuthButton from "../GoogleAuthButton";

const LoginForm = () => {
  return (
    <View>
      <GoogleAuthButton />
    </View>
  );
};

export default LoginForm;
